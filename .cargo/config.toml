# 工具链由仓库根 `rust-toolchain.toml` 固定为 stable-x86_64-pc-windows-gnu
# （本机无 MSVC Build Tools；GNU 工具链自带 rust-lld 与 self-contained 运行库）。
# 因此这里不再覆盖 build.target——保持"宿主即目标"，避免交叉编译语义带来的
# 构建脚本链接问题（build script 始终按宿主编译）。

[net]
git-fetch-with-cli = true
