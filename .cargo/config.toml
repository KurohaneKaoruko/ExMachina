# 工具链：项目根 `rust-toolchain.toml` 把 channel 固定为 `stable`，
# 实际 target triple 由 rustup 按宿主平台自动选择。
#
# 这里只保留网络层配置：使用系统 git 而非 libgit2，避免某些环境下
# 的协议兼容问题（具体见 cargo 文档 `net.git-fetch-with-cli`）。
[net]
git-fetch-with-cli = true
