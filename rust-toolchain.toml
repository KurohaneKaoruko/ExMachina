# 本项目固定使用 GNU 工具链：
#   本机未安装 MSVC Build Tools（无 link.exe），而 GNU 工具链自带 rust-lld 与
#   self-contained 运行库，纯 Rust 依赖可直接编译运行（已验证）。
#   若迁移到装有 VS Build Tools 的机器，可改为 "stable-x86_64-pc-windows-msvc"。
[toolchain]
channel = "stable-x86_64-pc-windows-gnu"
